import { fillSimilarCurrentPrices, parseB3PositionPage } from './finance-asset-import-model.mjs?v=5';
const MAX_BYTES=15*1024*1024,MAX_PAGES=30;
export async function readB3AssetsPdf(file,onProgress=()=>{}){
  if(!(file instanceof File)||!/.pdf$/i.test(file.name)||file.size<5||file.size>MAX_BYTES)throw new Error('Selecione um PDF de até 15 MB.');const bytes=new Uint8Array(await file.arrayBuffer());if(String.fromCharCode(...bytes.slice(0,5))!=='%PDF-')throw new Error('O arquivo não é um PDF válido.');const pdfjs=await import('./vendor/pdfjs/pdf.min.mjs');pdfjs.GlobalWorkerOptions.workerSrc='/vendor/pdfjs/pdf.worker.min.mjs';const pdf=await pdfjs.getDocument({data:bytes}).promise;if(pdf.numPages<1||pdf.numPages>MAX_PAGES)throw new Error('O PDF deve ter entre 1 e 30 páginas.');const assets=[];let classification=null;for(let pageNumber=1;pageNumber<=pdf.numPages;pageNumber++){onProgress(Math.round((pageNumber-1)/pdf.numPages*100),`Lendo página ${pageNumber} de ${pdf.numPages}…`);const page=await pdf.getPage(pageNumber),content=await page.getTextContent();const parsed=parseB3PositionPage(content.items,pageNumber,classification);assets.push(...parsed.assets);classification=parsed.classification;}const completed=fillSimilarCurrentPrices(assets);onProgress(100,`${completed.length} ativo(s) reconhecido(s).`);return completed;
}
